{
  "name": "penny-pal-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Telegram expense logging bot (TypeScript rewrite)",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "prepare": "husky",
    "typecheck": "tsc --noEmit -p tsconfig.typecheck.json",
    "verify": "npm run typecheck && npm test",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "telegraf": "^4.16.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/debug": "^4.1.13",
    "@types/node": "^22.10.2",
    "@types/node-fetch": "^2.6.13",
    "@types/safe-compare": "^1.1.2",
    "husky": "^9.1.7",
    "tsx": "^4.19.2",
    "typescript": "^5.2.2",
    "vitest": "^2.1.8"
  }
}
